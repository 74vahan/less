# Скопируй в .env и поменяй значения для прод-деплоя.
DB_USER=calendar
DB_PASSWORD=change-me-strong-db-pass
DB_NAME=calendar
JWT_SECRET=change-me-very-long-random-secret
# Порт, на котором nginx публикует сайт на сервере (рядом со study-platform:80)
HTTP_PORT=8088
